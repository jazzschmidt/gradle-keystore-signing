description = 'Signs artifacts using a keystore file'
group = 'com.github.jazzschmidt'

apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}